import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rmpondokmarisa.kasir',
  appName: 'RM.PONDOK MARISA',
  webDir: 'web'
};

export default config;
